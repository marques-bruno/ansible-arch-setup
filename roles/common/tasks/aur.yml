- name: Create AUR directory
  file: path=/home/{{ username }}/{{ aur.dir }}
        state=directory
        owner={{ username }}
        group={{ username }}

- name: Install AUR base packages
  kewlfft.aur.aur:
    name: "{{ aur.packages }}"
    user: "{{ username }}"

- name: Install rcm
  kewlfft.aur.aur: name=rcm user={{ username }} 

- name: Instal micro editor
  kewlfft.aur.aur: name=micro-git user={{ username }}
